{
  "name": "claudehud",
  "version": "0.1.0",
  "description": "A quiet companion for building WoW addons with Claude",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check renderer.js && node --check claude-service.js",
    "smoke": "electron . --smoke-test",
    "test": "node --test claude-service.test.js"
  },
  "devDependencies": {
    "electron": "^41.0.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.273"
  }
}
