{
  "name": "claudehud",
  "version": "0.1.19",
  "description": "A quiet companion for building WoW addons with Claude",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check renderer.js && node --check claude-service.js && node --check updater.js && node --check context-usage.js && node --check preferences.js && node --check game-capture.js && node --check message-format.js",
    "smoke": "electron . --smoke-test",
    "test": "node --test claude-service.test.js updater.test.js context-usage.test.js preferences.test.js game-capture.test.js message-format.test.js",
    "dist": "electron-builder --win nsis --x64"
  },
  "devDependencies": {
    "electron": "^41.0.0",
    "electron-builder": "^26.15.3"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.273",
    "electron-updater": "^6.8.9",
    "markdown-it": "^15.0.2"
  },
  "author": "Baz Studios LLC"
}
