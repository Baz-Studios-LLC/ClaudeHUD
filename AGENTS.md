# Release version policy

Keep all releases within 0.1.X. Increment only the patch number unless the user explicitly approves advancing beyond 0.1.X.

Batch changes locally. Do not push or publish updates until the user asks for a release.
